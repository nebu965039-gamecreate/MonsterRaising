# 後でやることリスト

決定済みだが後回しにしている作業、仕様が未決で保留にしている事項、設計書とロードマップの食い違いをまとめる。
着手時・決定時に更新すること(完了したら `[x]` にして、必要なら設計書へ反映する)。

## 仕様の決定待ち・数値の調整

- [ ] **成熟期への分岐の閾値を決めて実装する**(4.3節 条件3)
  - 現状は「優位な側のルートに進む」だけで、有効度の閾値・優位差は未設定(`Evolution.normalRoute`)。
  - 同数のときの扱いは未決。案:進化時に選択 UI で選ぶ(特別ルートと同じ仕組み)。
  - 閾値案(ミニゲームの実績値が出てから見直す): 「知力+筋力の合計 100 以上、かつ差が合計の 10% 以上」。設定値として持つ。
  - 目安:落ち物パズル中級(スコア 800)を 1 日 3 回、成熟期まで約 11 日続けると、知力・筋力それぞれ数百規模。
- [x] オフライン計算の方針を決定:24 時間上限は維持し、上限より前に下限へ届くよう減少速度を設定する(満腹度・清潔度とも 10 分ごとに -1)。
  上限は減少量にだけ効き、「0 が 24 時間継続」などは実際の時刻で数える。減少速度を変える場合は、この方針(上限前に 0 へ届く)を保つこと。
- [x] 放置中に進化の時刻が来ていた場合の扱いを決定:開いた(お世話した)時点の値で判定し、遡らない。1 回に進むのは 1 段階まで(設計書 4.3 に記載)。
- [x] 放置による死亡を実装(4.4節):満腹度 0 が 24 時間継続かつ清潔度 20 以下 → 新しい卵(何代目を +1、育成データはリセット)。
- [x] 寿命による世代交代を実装(4.5節):成熟期から 2 週間で寿命、長寿の秘薬(+1 週間)、先代の有効度 5% を引き継ぐ卵。
- [ ] 世代カウンターの画面表示の場所(設計書では「9節で検討」)。長寿の秘薬の入手(探索。Phase 8 相当)と所持数の管理。
- [ ] 死亡時の演出・お知らせ(現状はデモ画面のメッセージのみ)。
- [ ] 餌やり・掃除のクールダウン(4.2節。例:30 分に 1 回、または累積で効果逓減)。
- [ ] 機嫌の増加量の確定:餌やり +5 は暫定。掃除・ミニゲーム(+5〜15)・その他の加算量。
- [ ] 清潔度 0 の `dirty` と sad が重なった場合の優先順位(暫定は `dirty` 優先。4.6節に記載なし)。
- [ ] 初期ステータス(満腹度・清潔度 100、機嫌 50)と、満腹度・清潔度の減少速度(10 分ごとに -1)はプレイテストで調整。
- [ ] 各ステータスの数値バランス全般(設計書 12 節。プレイテスト後)。
- [ ] 特別ルートの進化(天候・アイテム・フレンド。4.3節)と、進化時の選択 UI。

## ミニゲーム(落ち物パズルの残り)

- [x] 落ち物パズルのコア・スコア→育成の反映・ハイスコア記録・画面(ロードマップ Phase 4)。ゲームロジックは独立した Gradle モジュール `:minigames:puzzle`(純粋な Kotlin)。
- [ ] **暫定値の確定**:落下速度、同時消去・コンボの倍率、積み上がったときの扱い、クリア後も続けるか(設計書 5.4.1)。
- [ ] **機嫌の食い違い**:4.1節「+5〜15、成績に応じ変動」と 5.4節「スコアに関わらず一定値」。現状は +10 の一定値。
- [ ] **難易度による落下速度の差**(5.2節「11節で調整」)と、ブロックの回転・当たり判定の微調整(5.2.1節)。
- [x] ミニゲーム共通基盤を実装:1 日のプレイ回数(3 回、ゲームごとに独立)、広告での追加(1 日 2 回まで。広告自体はデモ)、ミニゲーム券、有効度の 1 日の上限、持ち物と探索ポイント、クリア報酬(8.5節)。
- [x] 共通基盤の値を確定(2026-09-21):有効度の 1 日の上限は知力・筋力それぞれ 60、日付の切り替わりは 0 時、ミニゲーム券は全ミニゲーム共通。
- [ ] **持ち物の使い道**:ごはんを餌やりで消費する(10.2.1節)、長寿の秘薬の所持数と使用(現状はデモボタンのみ)、装備アイテムの効果計算(8.4節)・装着の仕組み・進化のアイテムルート。
- [x] **探索機能の基本を実装**(8節):ワールドマップ → 拠点 → 探索する/この場を離れる → 少し探索(1回)/じっくり探索(10回) → 出発の演出 → 探索中(残り〜)→ 成果の受け取り。ログインボーナス(1 日 100)。
- [ ] **探索の暫定値の確定**(設計書 8.6):同時に探索できるのは 1 か所、報酬は開始時に抽選、卵は不可、ログインボーナスはマップを開いたとき、海岸・山の導入文。
- [ ] **探索中のキャラクターの扱い**:メイン画面・ウィジェットから居なくなるか(お世話やミニゲームを制限するか)。
- [ ] 探索が終わったときの通知(ローカル通知、または FCM)。
- [ ] ワールドマップの本番:拠点のアイコン、現在地のキャラクター、未解放拠点のグレーアウト、自宅・ゲーム拠点への遷移(10.4節)。拠点の背景画像(現状は色のグラデーション)と、探索に出かけるときのアニメーションの本番。
- [ ] **フレンド限定ランキング**(方向性: 深夜 0 時に各ミニゲームのトップスコアを共有して表示。設計書 9.1節。Phase 6・7)。未決: 「トップスコア」がその日の最高か自己ベストか、0 時の基準(端末/サーバー)、0 時に起動していない場合の共有のタイミング。
- [ ] **リワード広告**(Phase 8):デモ用のボタンを実際の広告視聴に置き換える。
- [ ] フレンド限定ランキング(5.5節。Phase 6・7)。
- [ ] 操作性の調整(ボタンの大きさ・配置、スワイプ操作、振動・効果音など)。実機で確認して決める。
- [ ] Firebase Remote Config でのローテーション配信の対象にする(5.6節。Phase 9)。

## ミニゲーム(カード×NPC戦の残り)

- [x] カード×NPC戦のコア・モンスターカード・育成への反映・連勝数・勝ち上がり・画面(ロードマップ Phase 5)。ゲームロジックは独立した Gradle モジュール `:minigames:card`。
- [ ] **暫定値の確定**:賭けの扱い(双方が同じ枚数、払える範囲まで)、同点ラウンドはコインが動かない、配札(2 枚・NPC の 1 枚は伏せる)、山札(毎ラウンド 50 枚を切り直し)、機嫌 +10(設計書 6.5.1)。
- [ ] **有効度の加算量の調整**(様子見):カードは勝利で +8/+12/+16・無効試合 +1、落ち物パズルは `floor(スコア÷100)` を両軸に。1 日の上限(60)との釣り合いも、実際のプレイで確かめる。
- [ ] **モンスターカードの効果の見直し**(実際のプレイ感を見て変更する予定。設計書 6.4 は「例」のみ):成熟期のルートの判定(進化時のルートを記録していない)、「1 枚まで持ち込み」を 1 戦に 1 回の効果として実装している点。
- [ ] **NPC の見た目の本番ドット絵**(6.3節。黒い靄+赤い目、中級で口、上級で角)と、カードの意匠(天候アイコン)のドット絵。
- [ ] カードの属性の使い道(現状は意匠のみ)。
- [ ] 連勝数のフレンド限定ランキングとサーバー側の整合性チェック(6.6節。Phase 6・7)。
- [x] ミニゲーム3(壁破りゲーム。7節)を実装:独立モジュール `:minigames:wallbreak`、ミス無効、コンボ、育成への反映、画面。
- [ ] **壁破りゲームの暫定値の確定**(設計書 7.5.1):コンボの倍率(+0.1/段・上限 ×2.0)、ミス無効のときのコンボの扱い(0 に戻す)、クリアを突破数で判定しスコアはコンボ込みにしていること、不正解後は新しい指令に進むこと、筋力の換算(スコア÷2)。
- [x] 壁がはじけ飛ぶ演出を実装(破片の動きは `core/effects/BurstParticles`)。
- [ ] **壁のドット絵**(ユーザーが用意する):`assets/wallbreak/walls/<色名>.png` に置けば使われる。サイズ・向きを決めたら設計書 7.5.1 に追記する。破片を絵から切り出す演出にするかも検討。
- [ ] 壁への衝突(ミス)の演出、専用フレーム(設計書 3.4節の run / jump は予約)。
- [ ] 壁破りゲームのフレンド限定ランキング(7.6節。Phase 6・7)。
- [ ] 装備アイテムの効果を有効度に反映する(筋力の装備 +5% は、筋力値とミス無効の回数に影響する。8.4節)。

## 見た目・アセット

- [ ] **本番ドット絵への差し替え**:`eat_1` / `eat_2` / `dirty` / `idle_up` / `walk_a` / `walk_b` / `walk_mid` は仮絵(`tools/` の生成物)。
- [ ] **レイヤー素材(制作中)からのフレーム組み上げツール**:`art/fox/layers/<pose>/<part>.png` を重ね順・オフセット指定で完成画像に合成する。
- [ ] 進化段階ごとの見た目差分を 1 系統だけ作って検証する(ロードマップ Phase 2)。卵・幼年期・成長期Ⅰ・成長期Ⅱ・成熟期の各アセット。
- [ ] 卵の見た目(現状はデモ用に仮キャラをそのまま表示)。
- [ ] 設計書 3.4節のフレーム表を、レイヤー素材の確定後に見直す。
- [ ] ハートマークなどの演出用フレーム(10.2.1節)。

## 機能の実装(先のフェーズ)

- [ ] 餌やり(ドラッグ&ドロップ)・掃除(汚れをなでる。1 箇所 +20、清潔度に応じた汚れの個数)・なでる操作(10.2節)。
- [x] ウィジェット(ロードマップ Phase 3)を RemoteViews で実装:状態に応じた静止画、タップでアプリへ、WorkManager で 15 分ごとに更新、餌・掃除・なでるの単発タップ。
- [ ] ウィジェットの仕上げ:操作直後の演出(食事フレームなどを短く表示)、キャラクタータップでなでる、デザイン調整、フレンド来訪演出(Phase 7)、天候・拠点の反映。
- [ ] ウィジェットの掃除ボタンの効果量(現状は汚れ 1 箇所ぶん +20。4.2節「簡易版のみ」の内容を詰める)。
- [ ] デモ画面の仮想時計はウィジェット(実時間)と食い違う。ウィジェットの動作確認は仮想時計を使わずに行う。
- [ ] ミニゲームと有効度の加算・1 日の獲得上限(Phase 4・5)。
- [ ] 天候・拠点・探索・図鑑など(設計書 4.8・8・10.3〜10.5節)。

## 設計書・ロードマップの整合

- [ ] ロードマップ Phase 7 の「プレゼント送付/お世話代行/応援スタンプ」は、設計書 9 節では初回リリース見送り。どちらかに揃える。
- [ ] ロードマップ Phase 1 の「パーツ分割(体/頭/目/口/腕/脚/影/付属)」と設計書 3.2節(頭・胴体・左右の腕・左右の足・尻尾・背中)の食い違い。設計書を正とする方針で、ロードマップ側を更新する。
- [x] ロードマップに、ミニゲーム 3(壁破りゲーム)のフェーズ(Phase 5.5)を追記した。探索機能のフェーズは未追記(設計書 11 節)。

## 開発環境・運用

- [ ] Phase 1・2 のブランチを PR にして main へ取り込む(現状は feature/project-scaffold → feature/phase1-frame-player → feature/phase2-status-core が積み重なっている。`gh` は未導入)。
- [ ] 依存の更新(lint の通知のみ):AGP 9.4.1、Gradle 9.7.1、Compose BOM 2026.09.00、Kotlin 2.4.20。
- [ ] 実機での動作確認はユーザーが実施(CLAUDE.md の方針)。確認して気づいた点は随時ここへ。
