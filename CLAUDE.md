# MonsterRaising — Claude Code 開発指示書

このファイルはClaude Codeが本プロジェクトで作業する際に常に参照する指示書です。作業開始前に必ず読み込んでください。

## プロジェクト概要

ドット絵モンスター育成ウィジェットアプリ「MonsterRaising」のAndroidアプリ開発。

- ホーム画面ウィジェットに常駐するモンスターを、お世話・ミニゲームで育成する
- 詳細仕様はすべて `docs/basic-design.md`(基本設計書)に記載されている
- 開発はアジャイル形式で進める。基本設計書は「生きたドキュメント」であり、実装を進める中で齟齬や更新が必要な箇所が見つかった場合は、コードを書く前に指摘し、どう反映するか確認すること

## 最初に必ずやること

1. `docs/basic-design.md` を読み込み、該当セクションを把握してから着手する
2. 実装しようとしている機能が基本設計書のどの節に対応するか確認する
3. 基本設計書に記載がない・矛盾する仕様に気づいた場合は、憶測で実装せずに確認を求める

## 開発の進め方

- 別紙ロードマップ(基本設計書11節を参照)のPhase順に進める。現在の到達フェーズはユーザーに確認する
- 1回の作業は小さく区切り、レビューしやすい単位でコミットする
- 大きな機能は一気に実装せず、段階的に(例:ロジック→UI→結線)進める
- 後回しにした作業・未決事項は `docs/todo.md` に記録する。着手前に確認し、決定・完了したら更新する

## 検証方針

- **Lint・ビルドエラーのチェックは実装のたびに必ず行う**(例:`./gradlew lintDebug`、`./gradlew compileDebugKotlin` 相当のコマンド)。エラー・警告は都度解消してから次に進む
- **実機での動作確認は行わない**。実機テストはユーザー自身のタイミングで実施する
- **エミュレータでの動作確認は、必要に応じてClaude Codeが行ってよい**(ビルド後の起動確認、画面遷移、操作の一連の流れなど)。既存のAVD(Pixel_10 / Pixel_9)を使う。確認後はエミュレータを終了する
- ユニットテストは、UIに依存しない計算ロジック(ステータス計算・進化判定・スコア計算など)を優先して書く

## トークン効率の方針

このプロジェクトはできるだけ少ないトークンで開発を進める方針。以下を徹底すること。

- 変更していないファイルを繰り返し読み直さない
- コード全文をチャット上に貼らず、ファイルへの直接編集(diff相当の変更)で完結させる
- 説明は簡潔に。自明なコードの逐次解説はしない
- 大きなファイルを全文書き直すのではなく、部分編集で済む場合はそちらを優先する
- 確認・提案は要点のみ。冗長な前置き・繰り返しの要約はしない

## コーディング方針

- 言語:Kotlin / UI:Jetpack Compose / ウィジェット:RemoteViews または Glance
- 将来のiOS展開(基本設計書1.2.1節)に備え、UIやAndroid固有APIに依存しない計算ロジック(ステータス計算・進化判定・スコア計算・装備効果計算など)は、UI層から独立したクラス・関数として実装する
- キャラクターのアニメーションは差分画像切り替え方式(基本設計書3章)。パーツ単位のリアルタイム合成は行わない
- 外部サービスはFirebase(Auth / Firestore / Cloud Functions / FCM / Remote Config)、天気はOpenWeatherMap、広告はAdMob(基本設計書2節)

## バージョン管理

- Gitで管理し、GitHubのリモートリポジトリにpushする(リポジトリ名:MonsterRaising)
- コミットは意味のある単位に分け、コミットメッセージは変更内容が分かる日本語または英語で簡潔に書く
- 機能追加・修正の作業単位でブランチを分けることを基本とする(main直コミットは避ける)

## ディレクトリ構成の目安

```
MonsterRaising/
├── CLAUDE.md
├── docs/
│   └── basic-design.md   # 基本設計書(このリポジトリでの正)
├── app/                  # Androidアプリ本体
├── minigames/            # ミニゲームのロジック(ゲームごとに独立した純粋なKotlinモジュール)
└── ...
```

## 不明点があるとき

仕様の解釈に迷った場合、または基本設計書に載っていない判断が必要な場合は、実装を進める前に必ずユーザーに確認すること。基本設計書12節相当の「未決定事項」に該当する内容は特に注意する。
